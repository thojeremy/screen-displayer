{
  "name": "screen-displayer",
  "version": "1.0.0",
  "description": "A simple screen image displayer written with Electron",
  "main": "main.js",
  "scripts": {
    "test": "echo TODO",
    "package:mac": "electron-packager . --overwrite --platform=darwin --arch=x64  --prune=true --out=release-builds",
    "package:windows": "electron-packager . --overwrite --platform=win32 --arch=ia32  --prune=true --out=release-builds",
    "package:linux": "electron-packager . --overwrite --platform=linux --arch=x64  --prune=true --out=release-builds"
  },
  "keywords": [],
  "author": "Jérémy Tho",
  "license": "GPL-3.0",
  "devDependencies": {
    "electron": "^12.0.4",
    "electron-packager": "^15.2.0"
  }
}
